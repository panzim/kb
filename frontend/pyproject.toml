[project]
name = "frontend"
version = "0.1.0"
description = "PANZIM frontend"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "pydantic",
    "uvicorn>=0.40.0",
    "requests>=2.32.5",
]
