[project]
name = "Panzim"
readme = "Readme.md"
version = "0.1.0"
dependencies = [] # dependencies are in workspaces for ease of biulding containers

[tool.uv.workspace]
members = ["backend", "frontend"]
